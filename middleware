ถ้าในไฟล์มีโค้ดเยอะมาก 1000 บรรทัดแบบที่อาจารย์ให้
ให้หาส่วนพวกนี้:
func main() {
แล้วมองหา:
http.ListenAndServe(
หรือ
router.Run(
หรือ
r.Run(
แล้วเอา middleware ไป “ครอบ” ตรงนั้น
----------------------------------------------------------------------
เช่นเดิมอาจเป็น:
http.ListenAndServe(":8080", mux)
แก้เป็น:
http.ListenAndServe(":8080", middleware(mux))
-----------------------------------------------------------------------

|client|
|  ↓   |
|middleware|
| ↓     | 
|handler|


